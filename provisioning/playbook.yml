---
- name: Configure victim
  hosts: victim
  become: true
  roles:
    - role: user-access
      user_access_username: seed
      user_access_password: seed
      user_access_sudo: False   # deliberately NOT full sudo - see roles/victim/tasks/main.yml
    - role: chrony
    - role: victim
